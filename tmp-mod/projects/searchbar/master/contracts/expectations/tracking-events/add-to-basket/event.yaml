---
name: add-to-basket2
category: product-interactions
fieldsSchema:
  type: object
  properties:
    productSku:
      type: string
    page:
      type: string
      enum: ["homepage", "catalog", "pdp"]
  required:
    - productSku